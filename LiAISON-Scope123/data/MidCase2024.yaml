data_directories:
  reeds_data: reeds_data/
input_filenames:
  creation_inventory: na # These inventories are not used for LCA. Its used for building large scale activity systems inside ecoinvent, such as ReEDS Grid mixes. hese inventories are used for ReEDS Grid mix operations specifically. Should be harcoded.
  modification_inventory: na # These inventories are used for ReEDS Grid mix operations specifically. Should be harcoded. 
  modification_inventory_us: na # These inventories are used for ReEDS Grid mix operations specifically. Should be harcoded.

